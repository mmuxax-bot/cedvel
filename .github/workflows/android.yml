name: Android CI/CD (Native Flutter)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
        
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.22.2'
        channel: 'stable'
        
    - name: Asılılıqları Yüklə
      run: flutter pub get
      
    - name: Android Build Plugin Xətasını Düzəlt
      run: |
        if [ -f "android/app/build.gradle" ]; then
          sed -i "s/com.android.library/com.android.application/g" android/app/build.gradle
        fi
        
    - name: APK Derlə
      run: flutter build apk --release
      
    - name: Hazır APK-nı Yüklə
      uses: actions/upload-artifact@v4
      with:
        name: nibras-native-apk
        path: build/app/outputs/flutter-apk/app-release.apk
